# Contributing

Run `python -m unittest discover -s tests -v`. Add synthetic regression cases for changes to authorization or workflow transitions. Keep private data, tokens and community records out of commits. Preserve explicit consent and human approval. Contributions use Apache-2.0.
